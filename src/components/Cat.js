import React from "react";
import { Text } from "react-native";

const Cat = ()=>{
    return (
        <Text>Cat!!!!</Text>
    )
}
const PI = 3.14;
const squaredNumber = (a)=>{
    return Math.sqrt(a);
}

export {PI, squaredNumber};
export default Cat;

